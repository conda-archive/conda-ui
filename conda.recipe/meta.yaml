package:
  name: conda-ui
  version: 0.1.0

build:
  number: {{ environ.get('BINSTAR_BUILD', 1) }}
  entry_points:
    - conda-ui = conda_ui:main

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - flask
    - conda

about:
  home: https://github.com/ContinuumIO/conda-ui
  license: BSD License
